class Solution {
public:
    int arraySign(vector<int>& nums) {
        int c=2;

        for(int i:nums)
        {
            if(i==0)
            return 0;
            if(i<0)
            c++;
        }

        if(c%2!=0)
        return -1;
        else
        return 1;
    }
};
